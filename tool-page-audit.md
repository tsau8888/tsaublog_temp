# 工具頁回應與版面檢查紀錄

## 載入回應

本機開發服務在跟隨尾端斜線重新導向後，所有工具頁均回傳 HTTP 200。首次檢查的回應時間分別為：工具總覽 0.104 秒、QR Code 0.959 秒、編解碼 0.355 秒、加解密 0.532 秒、雜湊 0.410 秒、圖片浮水印 0.359 秒。

## 互動與版面

QR Code 工具以網址輸入 `https://example.org/tools` 實測後，預覽即時更新且無錯誤。行動版檢查發現 QR 預覽 SVG 維持固定 400px 寬度而被外層裁切；已調整為依容器寬度縮放。其他工具頁在桌面與 375px 行動視窗下均未發現控制項重疊、文字裁切或主要內容橫向溢位。

雜湊工具以 `abc` 實測後，即時產生的 MD5、SHA-1、SHA-256 與 SHA-512 結果分別符合公開已知測試向量；結果卡片在瀏覽器實際介面中正常展開，未影響內容區寬度或操作回應。

編解碼工具以 `Tsau 工具 🛠` 實測後，立即輸出 `VHNhdSDlt6Xlhbcg8J+boA==`，輸入與輸出欄位均維持可讀且未出現版面跳動。

加解密工具以固定 AES-256-GCM 金鑰、12 位元組 IV 與文字 `工具頁回應測試` 實測後，立即產生 `gcm1:IV:密文` 輸出；演算法選擇、金鑰與 IV 欄位、文字輸入輸出區均正常呈現。

圖片浮水印工具的上傳區、文字設定、顏色、大小、透明度、角度與位置控制項在預設畫面均正常排列。自動化上傳介面只顯示可點擊的標籤而未暴露隱藏檔案欄位，因此改用受控瀏覽器測試資料驗證圖片預覽流程。

受控 SVG 圖片資料已成功觸發浮水印工具的檔案變更流程。圖片預覽、下載浮水印圖片按鈕，以及隨預覽出現的水平與垂直偏移控制項均正常顯示，未發現載入或版面異常。
